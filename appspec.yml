version: 0.0
os: linux
files:
  - source: /
    destination: /Django_Chatapp
hooks:
  BeforeInstall:
    - location: scripts/before_install.sh
      timeout: 300
      runas: ubuntu
  AfterInstall:
    - location: scripts/clean_old_files.sh  # NEW: This will delete old files before deployment
      timeout: 300
    - location: scripts/after_install.sh
      timeout: 300
      runas: ubuntu
  ApplicationStart:
    - location: scripts/start_server.sh
      timeout: 300
      runas: ubuntu
